/**
 * 公用方法：顯示錯誤訊息。
 */
function errMsg(eMsg) {
	// 目前沒有其他第三方套件，所以使用基本的alert()方法
	alert(eMsg);
}

/**
 * 公用方法：顯示一般訊息。
 */
function showMsg(msg) {
	// 目前沒有其他第三方套件，所以使用基本的alert()方法
	alert(msg);
}

/**
 * 公用方法：顯示系統訊息。
 */
function sysMsg(sMsg) {
	// 目前沒有其他第三方套件，所以使用基本的alert()方法
	alert(sMsg);
}
